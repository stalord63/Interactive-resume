name: Deploy React App to EC2 using Docker and SSH

on:
  push:
    branches: [fixes_1.0]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t react-app .

      - name: Save Docker Image as tar
        run: docker save react-app -o react-app.tar

      - name: Copy Docker Image to EC2
        run: |
          echo "${{ secrets.EC2_PRIVATE_KEY }}" > key.pem
          chmod 600 key.pem
          scp -o StrictHostKeyChecking=no -i key.pem react-app.tar ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/home/${{ secrets.EC2_USER }}/react-app/

      - name: SSH and deploy Docker container
        run: |
          echo "${{ secrets.EC2_PRIVATE_KEY }}" > key.pem
          chmod 600 key.pem
          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            cd ~/react-app || exit 1
            docker load -i react-app.tar
            if [ $(docker ps -aq -f name=react-app) ]; then
              docker stop react-app
              docker rm react-app
            fi
            docker run -d \
              --name react-app \
              -p 80:80 -p 443:443 \
              -v /etc/letsencrypt/live/stalord.in:/etc/letsencrypt/live/stalord.in:ro \
              -v /etc/letsencrypt/archive/stalord.in:/etc/letsencrypt/archive/stalord.in:ro \
              -v /etc/letsencrypt/renewal:/etc/letsencrypt/renewal:ro \
              -e DOMAIN=stalord.in \
              -e EMAIL=admin@stalord.in \
              react-app
          EOF

